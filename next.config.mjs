/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true
  }
  // Se você precisar de outras configurações, adicione-as aqui.
}

export default nextConfig
